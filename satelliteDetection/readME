Folder includes main training script + results I found interesting

- This project takes in satellite images and draws bounding boxes around identifiable vehicular objects and outputs a confidence interval as to how sure the model is that said vehicle is what its ground truth label suggests.
